//- MENU AND MENUBAR GENERATORS

//- Menu generator

mixin menu(menuLink, total, menuClass)
  - var n = 0;
  ul(class=["menu", menuClass])
    while n < total
      li
        a(href="#/")= menuLink
          = " "
          = (n++ + 1)

//- Menubar generator

mixin menubar(menuLink, total, menuClass)
  - var n = 0;
  ul(class=["menubar", menuClass])
    while n < total
      li
        a(href="#/")= menuLink
          = " "
          = (n++ + 1)
