<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Layout: Classic Grid | Codebase</title>

  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/holder/2.9.1/holder.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.6.3/css/font-awesome.min.css">
  <link rel="stylesheet" href="theme/css/styles.css">
  

</head>

<body>

  <!-- @import "site-menubar.kit" -->

  <header class="codebase-docs__header">
    <span><a href="layout.html">Layout</a>:</span>
    <h1>Classic Grid</h1>
  </header>

  <!-- @import "site-sidebar.kit" -->

  <p>
    Example:
  </p>

  <section class="grid grid--gutter">
    <div class="col col-md--span-8 col-lg--push-4 deco-b-thin-solid">Article
      <section class="grid grid--gutter grid--nested">
        <div class="col col-sm--span-4 deco-b-thin-solid">Nested</div>
        <div class="col col-sm--span-4 deco-b-thin-solid">Nested</div>
        <div class="col col-sm--span-4 deco-b-thin-solid">Nested</div>
      </section>
    </div>
    <div class="col col-md--span-4 col-lg--pull-8 deco-b-thin-solid">Aside</div>
  </section>
  <section class="grid grid--gutter">
    <div class="col col-sm--span-7 col--centered deco-b-thin-solid">Centered</div>
  </section>

  <pre><code>&lt;section class="grid grid--gutter"&gt;
  &lt;div class="col col-md--span-8 col-lg--push-4"&gt;
    Article

    &lt;section class="grid grid--gutter grid--nested"&gt;
      &lt;div class="col col-sm--span-4"&gt;
        Nested
      &lt;/div&gt;
      &lt;div class="col col-sm--span-4"&gt;
        Nested
      &lt;/div&gt;
      &lt;div class="col col-sm--span-4"&gt;
        Nested
      &lt;/div&gt;
    &lt;/section&gt;

  &lt;/div&gt;
  &lt;div class="col col-md--span-4 col-lg--pull-8"&gt;
    Aside
  &lt;/div&gt;
&lt;/section&gt;

&lt;section class="grid grid--gutter"&gt;
  &lt;div class="col col-sm--span-7 col--centered"&gt;
    Centered
  &lt;/div&gt;
&lt;/section&gt;
</code></pre>

  <table class="table">
    <thead>
      <tr>
        <th style="width:160px">Class</th>
        <th>Explanation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>.grid</td>
        <td>The containing class. Sets the max width (from variable $grid-max-width), does centering by auto margin right and left, and includes a clearfix.</td>
      </tr>
      <tr>
        <td>.grid--gutter</td>
        <td>Gives right and left padding to all immediate child block elements (i.e. cols).</td>
      </tr>
      <tr>
        <td>.grid--nested</td>
        <td>Gives negative right and left margin to the grid container equal to the gutter width, so that a nested grid is flush right and left inside their containing col.</td>
      </tr>
      <tr>
        <td>.col</td>
        <td>Float left.</td>
      </tr>
      <tr>
        <td>.col--centered</td>
        <td>No float. Instead, centering is accomplished by auto margin right and left.</td>
      </tr>
      <tr>
        <td>.col-xs--*<br>
          .col-sm--*<br>
          .col-md--*<br>
          .col-lg--*
        </td>
        <td>Controlling the colukm behaviour at different breakpoints.<br>
          Except for .xs--* all columns expand to 100% of their container width below their breakpoint.</td>
      </tr>
      <tr>
        <td>*--span-*</td>
        <td>Column spanning (at different breakpoints), as calculated out of max $grid-max-cols (default is 12).</td>
      </tr>
      <tr>
        <td>*--push-*</td>
        <td>Column pushing to the right (at different breakpoints), as calculated out of max $grid-max-cols (default is 12).</td>
      </tr>
      <tr>
        <td>*--pull-*</td>
        <td>Column pullling to the left (at different breakpoints), as calculated out of max $grid-max-cols (default is 12).</td>
      </tr>
      <tr>
        <td>*--offset-*</td>
        <td>Column offsetting from the left (at different breakpoints), as calculated out of max $grid-max-cols (default is 12).</td>
      </tr>
    </tbody>
  </table>

  <!-- @import "site-footer.kit" -->

</body>

</html>
